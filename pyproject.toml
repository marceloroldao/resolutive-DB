[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "resolutive-db"
version = "1.0.0"
description = "Resolutive Database Engine (BDR) v1 stable engine"
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "Marcelo Roldão Matos" }
]
license = { file = "LICENSE" }

[tool.setuptools]
packages = ["bdr"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
