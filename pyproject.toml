[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "resolutive-db"
version = "1.2.0rc1"
description = "Resolutive Database Engine (BDR) v1.2 RC1 with atomic Python bridge and bulk-read optimizations"
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "Marcelo Roldão Matos" }
]
license = { file = "LICENSE" }

[tool.setuptools]
packages = ["bdr"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
