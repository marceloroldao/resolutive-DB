# BDR v1.2.0-rc4 validation record

Status: release-candidate freeze validation

## Baseline

- Functional storage/ABI baseline: `d09914b85646353d8fd004ccf99e96a94fab9eef`.
- Published RC1 commit remains `eb77ad7286f234243ca1ed1a2af2d55df8c12238` with DOI `10.5281/zenodo.22784729`.
- Existing `v1.2.0-rc1`, `v1.2.0-rc2` and `v1.2.0-rc3` tags are immutable.
- RC4 adds no storage-semantic change after `d09914...`; staging commits are metadata/docs/CI only.

## Required gates

- root/Python version aligned to `1.2.0rc4`;
- V97 and V98 release audits pass while stable citation/Zenodo metadata remains on v1.1.0;
- Atomic C ABI reports version 2;
- header and shared library expose `bdr_atomic_c_clear`;
- `v1_atomic_c_api_clear` passes;
- complete API v86 CTest passes;
- cold reopen after logical clear passes;
- legacy BDW4 bounded-recovery tests remain green;
- historical RC1/RC2/RC3 tags remain present and untouched.

## Cross-repository gate

After BDR RC4 validation, Memoria.ia must pin the exact frozen RC4 commit and run its native/mobile/product/cognitive regression matrix before the next Memoria.ia freeze.

## Freeze rule

No storage-semantic change is permitted after final validation. Publication metadata may only name the exact green freeze commit. No prior RC tag, release, DOI or artifact may be moved, deleted or overwritten.
