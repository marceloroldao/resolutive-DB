# BDR v1.2.0-rc3 validation record

Status: release-candidate freeze validation

## Baseline and scope

- Published RC1 and RC2 tags/releases remain immutable.
- RC3 candidate includes the bounded legacy BDW4 dual-handle recovery compatibility validated on the v1.2 line.
- Historical legacy prefix accepted only at WAL head: `1,1,2,3...`.
- Invalid repetition/gaps such as `1,1,1`, `1,2,2` and ordinary sequence gaps remain rejected.

## Evidence required before freeze

The release branch must pass the complete PR gate set, including V99/V100 evidence closure, BDR CI, durability, crash recovery, public API compatibility, Python atomic bridge, Memoria benchmarks and packed-key/output probes.

Production cross-version evidence already demonstrated preservation of 300 historical episodes through upgrade, cold reopen and rollback.

## Freeze rule

No storage-semantic change is permitted after validation. The freeze branch may publish only if its commit contains the validated release commit as an ancestor. RC1 and RC2 are never moved, retagged, deleted, or overwritten.
